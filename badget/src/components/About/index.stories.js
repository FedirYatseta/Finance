import React from 'react';

import About from '.';

export default {
    title: 'Example/About',
    component: About,
};

const Template = (args) => <About {...args} />;

export const Item = Template.bind({});
Item.args = {


};